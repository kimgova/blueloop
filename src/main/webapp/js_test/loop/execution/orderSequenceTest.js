/**
 * 
 */

QUnit.test("save-sequence", function( assert ) {
	var e = {data:{sequenceCollection:[]}}
	var sequencePaneView = new cbbSequenceTabView();
	sequencePaneView.orderSeqId = 1;
	//sequencePaneView.saveSequence(e);
	assert.equal( 0, 0, "Zero, Zero; equal succeeds" );
});